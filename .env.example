# PostgreSQL connection — copy to .env and fill in real values
# Docker: replace localhost with host.docker.internal
# Port is 5433 (non-standard)
DB_URL=jdbc:postgresql://host.docker.internal:5433/YOUR_DB_NAME
DB_USER=YOUR_DB_USER
DB_PASS=YOUR_DB_PASS
